# 검수 범위와 예제 결과

이 기록은 저장소에 포함한 예제와 도구의 확인 범위를 설명한다. 자동 검사 통과와 실제 수업의 이해도·실습 성공은 구분한다.

## 다시 확인하기

저장소 루트에서 실행한다.

```bash
python -m pip install -r requirements.txt
python -m unittest discover -s tools/tests -v
python tools/check_examples.py
python tools/build_network_diagrams.py --out build/diagrams
```

마지막 명령에는 한글 글꼴이 필요하다. [그림 도구 안내](diagrams.md)를 참고한다. 검사 로그와 HTML 미리보기는 `build/day3/`에 생성되며 Git에는 포함하지 않는다.

## 2026-09-06 설명 흐름 수정 후 확인한 결과

6개 교시의 본문·목차를 재구성하고 기존 스킬과 하네스에 같은 검수 기준을 반영했다. 기본 설명과 선택 보충을 구분하고, 예제 바로 뒤에 해설을 배치했다. 자동 검사는 의미상 주제 전환이나 설명의 깊이를 판정하지 않으며, 해당 항목은 읽기 검수에 남긴다.

- 단위 테스트 42개 중 41개 통과, 1개 건너뜀. 건너뛴 항목은 설치된 한글 글꼴이 필요한 그림 생성 검사다. 이번에는 기존 그림을 다시 생성하지 않았다.
- 6개 JSON의 구조 검사, Markdown·HTML 생성, 하네스와 린터 실행 완료. 하네스 FAIL 0, 린터 ERROR 0이다.
- 원문의 용어·별칭 170개를 유지했다. 용어 도입 문구는 재배치한 실제 본문에 맞췄으며, 필수 본문이 접힌 보충의 정의에 의존하지 않는지도 검사했다.
- 기존 실행 코드·명령의 입력과 출력 예시는 보존했다. 6교시 Markdown 보고서 양식은 서로 다른 사건의 관찰을 섞지 않도록 수정했다. 5교시에는 시작 파일이 없는 학생을 위한 선택 준비 절차를 추가했다.
- 본문 그림 18개의 참조를 보존했다. PNG·SVG 원본은 변경하지 않았고, 이미지 용어 목록은 실제 SVG에서 다시 추출해 누락된 반복 레이블까지 일치시켰다.
- 독립 읽기 검수에서 확인한 Simulation 진행 버튼, 정상 시작 파일 준비, 그림의 PC 배치, 6교시 사건 구분을 수정했다. 해당 네 항목은 재검수에서도 해결된 것으로 확인했다.
- 기존 스킬의 메타데이터 검사 통과. 새 구조 검사에는 보충 렌더링·실행·용어 도입·목차·중복 설명 등에 관한 회귀 검사 9개를 추가했다.

| 교시 | 하네스 PASS | FAIL | SKIP | REVIEW | 린터 ERROR | WARN |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 34 | 0 | 1 | 0 | 0 | 2 |
| 2 | 18 | 0 | 1 | 0 | 0 | 2 |
| 3 | 35 | 0 | 1 | 0 | 0 | 2 |
| 4 | 40 | 0 | 1 | 0 | 0 | 0 |
| 5 | 34 | 0 | 2 | 1 | 0 | 0 |
| 6 | 26 | 0 | 3 | 0 | 0 | 2 |

PASS 187개는 용어 170개, Python 출력 비교 7개, 구조 확인 10개다. SKIP 9개는 실행 대상이 아닌 언어의 `code` 요소다. `command`와 GUI 실습 절차는 Python 하네스 실행 대상이 아니며, 이 수치에 모두 포함되는 것은 아니다.

5교시의 REVIEW 1개는 A 레코드 설명의 반복이다. 정상 파일을 새로 만드는 선택 준비 절차와, 그 절차를 건너뛰는 학생도 읽는 필수 본문에서 각각 필요하므로 유지했다. 반복 목적을 검토한 결과이며, 다른 중복 경고를 일괄 허용한다는 뜻은 아니다. 린터 WARN 8개의 의미는 아래에 기록했다.

### 수업 시간 배분 검토

아래는 교시당 50분을 가정한 운영 초안이며 실제 수업에서 측정한 시간은 아니다. 모든 교시에 같은 시간표를 반복하는 대신 활동의 차이에 맞춰 배분했다. 속도가 다르면 선택 보충·추가 문제를 조정하고 핵심 확인까지는 마친다.

| 교시 | 시간 배분 초안 |
| --- | --- |
| 1 | 서버 역할 8분, DNS와 웹 흐름 15분, 조회 출력 해석 12분, 실패 비교·문제 15분 |
| 2 | 이름 관리 계층 10분, 조회 주체와 흐름 15분, 모형 코드 해석 10분, 비교·문제 15분 |
| 3 | 레코드·캐시·TTL 15분, 위협과 판단 기준 10분, 로그 읽기 10분, 분석 문제 15분 |
| 4 | 배치·연결 8분, VLAN·라우터 15분, 주소·ping 10분, DNS·웹 확인·저장 17분 |
| 5 | 정상 기준 확인 7분, 변경 구성 13분, Simulation 관찰 15분, 복구 8분, 의미·정리 7분 |
| 6 | 판단 연습 20분, 사건별 근거 정리 10분, 보고서 작성 15분, 상호 확인 5분 |

5교시의 정상 구성 재제작은 시작 파일이 없을 때 사용하는 준비 활동이다. 4교시의 구성 시간이 추가로 필요하므로 5교시 본 활동 50분에 모두 포함된다고 보지 않는다.

### 이번 확인의 한계

Windows 명령과 Packet Tracer 9.0.1 실습은 이 환경에서 실행하지 않았다. `.pkt` 파일은 학생이 절차를 수행하며 만드는 결과물이고 저장소에 제공하지 않는다. Simulation에서 웹 요청을 완료하려면 이벤트 진행이 필요하다는 절차는 Cisco 공식 도움말과 대조했다. 실제 수업 환경의 동작과 소요 시간은 별도로 확인한다.

HTML 파일은 생성했으나 이번 환경의 브라우저 접근 정책으로 화면 미리보기를 열지 못했다. 브라우저 화면 검수나 Notion 게시 후 검수를 완료했다고 보지 않는다. Notion에 반영할 때는 원본 페이지를 먼저 읽고 수정한 뒤 다시 조회해 비교한다.

## 최초 공유본 준비 시 확인한 결과 (수정 전 기록)

로컬 macOS·Python 3.9.6 환경에서 다음을 확인했다. 새 환경에서는 Python 3.10 이상을 권장한다. GitHub 자동 검사 설정은 Ubuntu·Python 3.10/3.12에서 단위 테스트와 예제 검수를 실행한다. 실제 실행 결과는 저장소의 Actions에서 확인한다.

- 단위 테스트 33개 통과: 용어 검사 23개, 교안 렌더링·문체 검사 5개, 그림 생성기 5개.
- 예제 6개의 구조 검사·Markdown 변환·하네스·린터·HTML 생성 완료.
- 용어 항목 170개와 Python 실행 예제 7개 통과.
- PNG 19개·SVG 19개·전체 그림 미리보기 1개를 실제 생성하고 재실행했다. PNG 파일 유효성과 SVG 구문을 확인했다.
- 스킬 메타데이터 검사 통과. 개인 로컬 경로·인증 토큰·실제 Notion 페이지 및 업로드 식별자가 공유 파일에 남지 않았는지 확인했다.

| 교시 | 하네스 PASS | FAIL | SKIP | 린터 ERROR | WARN |
| --- | ---: | ---: | ---: | ---: | ---: |
| 1 | 32 | 0 | 1 | 0 | 2 |
| 2 | 16 | 0 | 1 | 0 | 2 |
| 3 | 33 | 0 | 1 | 0 | 2 |
| 4 | 39 | 0 | 1 | 0 | 0 |
| 5 | 32 | 0 | 1 | 0 | 0 |
| 6 | 25 | 0 | 3 | 0 | 2 |

PASS 수에는 용어 항목과 실행 출력 비교가 함께 포함된다. SKIP 8개는 Python 실행 대상이 아닌 언어의 `code` 요소다. Windows 명령이나 Packet Tracer 실습이 성공했다는 뜻은 아니다.

## 남아 있는 WARN의 의미

1·2·3·6교시에는 각각 ‘실행 코드 블록 수가 기준보다 적음’과 ‘함정·에러 시연 횟수가 기준보다 적음’이 표시된다. 린터의 기본 기준은 코딩 수업을 전제로 한다. 이 예제는 네트워크 개념, 조회 결과 해석과 관찰 활동도 포함하므로 코드 블록 수만으로 활동량을 판단하지 않는다.

이 8개 WARN은 예제의 성격에 따른 검토 대상으로 남겼다. 새 교안에서도 같은 경고를 일괄 무시하지 말고, 수업 시간에 맞는 활동량과 실패 결과를 해석하는 기회가 충분한지 읽고 판단한다. 구조 오류·하네스 FAIL·린터 ERROR는 해결한 뒤 사용한다.

## 별도로 확인할 것

- 용어 검사는 `learning_terms`에 등록한 항목만 검사한다. 새 용어 누락, 어려운 정의, 같은 문단 안의 세부 설명 순서는 사람이 읽어 확인한다.
- 그림의 `text_content`는 실제 그림 레이블과 일치해야 한다. 메타데이터만 고쳐 검사를 통과시키지 않는다.
- Python 하네스는 교안 코드를 실제 실행한다. 출처를 신뢰할 수 있는 교안에 사용한다.
- DNS 실제 응답은 시간과 접속 환경에 따라 달라질 수 있다. Windows 명령과 Packet Tracer 동작은 사용할 수업 환경에서 직접 확인한다.
- 그림은 macOS의 한글 글꼴로 확인했다. Windows·Linux 및 다른 글꼴의 실제 화면은 별도 확인이 필요하다. 글자 폭에 따라 줄바꿈과 배치를 조정한다.
- Notion 업로드와 저장 후 비교는 사용자의 Notion 연결에서 수행한다. 로컬 HTML 검수가 Notion에 저장된 결과의 검수를 대신하지 않는다.

최종 읽기 검수에는 [검수 기준](../tools/judge_checklist.md)과 [스킬의 발행 절차](../.agents/skills/lesson-page/references/validation-and-publishing.md)를 함께 사용한다.
